// Generated by CoffeeScript 1.12.7
(function() {
  var puzzleGame;

  puzzleGame = (function() {
    function puzzleGame(debug) {
      this.debug = debug;
    }

    puzzleGame.prototype.init = function() {
      if (this.debug) {
        return console.log('debug enabled');
      }
    };

    return puzzleGame;

  })();

  jQuery(document).ready(function() {
    $('#test').click(function() {
      var col, currentGame, i, imgHeight, imgWidth, pos, row, tile, tileHeight, tileWidth, tiles, xsplit, ysplit;
      xsplit = 4;
      ysplit = 4;
      tiles = xsplit * ysplit;
      imgWidth = $('#defaultImg').width();
      imgHeight = $('#defaultImg').height();
      tileWidth = imgWidth / xsplit;
      tileHeight = imgHeight / ysplit;
      console.log(imgWidth, imgHeight, tileWidth, tileHeight);
      i = 0;
      col = 0;
      while (col < ysplit) {
        row = 0;
        while (row < xsplit) {
          tile = $('#defaultImg').clone();
          $(tile).draggable();
          pos = $('#defaultImg').position();
          $(tile).css({
            'background-position': -row * tileWidth + 'px ' + -col * tileHeight + 'px',
            'width': tileWidth,
            'height': tileHeight
          });
          $('main').append(tile);
          i++;
          row++;
          console.log('created', i, 'tile', i * tileWidth + 'px ' + i * tileHeight + 'px', row, col);
        }
        col++;
      }
      $('#defaultImg').hide();
      currentGame = new puzzleGame('true');
      currentGame.init();
    });
  });

}).call(this);
